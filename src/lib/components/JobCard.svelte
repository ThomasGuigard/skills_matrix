<script lang="ts">    import "../../app.css";    import {Fa} from "svelte-fa/src/fa.svelte";    import {faBrain, faHands, faLightbulb, faMinus, faPlus, faUser} from "@fortawesome/free-solid-svg-icons";    export let job: {      title: string,      motivations: string[],      skills: {name: string, type: string}[],      highlights: string[],      downsizes: string[],    };</script><div class="max-w-2xl rounded overflow-hidden shadow-lg">    <h2 class="font-bold text-xl mb-2">{job.title}</h2>    <div>        <div>            <Fa icon="{faUser}"/>            <h3 class="font-bold text-l mb-1">Pour qui :</h3>        </div>        <ul>            {#each job.motivations as motivation}                <li class="text-gray-700 text-sm">{motivation}</li>            {/each}        </ul>    </div>    <div>        <div>            <Fa icon="{faBrain}"/>            <h3 class="font-bold text-l mb-1">Compétences :</h3>        </div>        <ul>            {#each job.skills as skill}                <li>                    {#if skill.type === "Savoir être"}                        <Fa icon="{faLightbulb}"/>                    {:else }                        <Fa icon="{faHands}"/>                    {/if}                </li>            {/each}        </ul>    </div>    <div class="grid grid-cols-2 divide-x">        <div class="flex-col">            <Fa icon="{faPlus}"/>            <ul>                {#each job.highlights as highlight}                    <li class="text-gray-700 text-sm">{highlight}</li>                {/each}            </ul>        </div>        <div class="flex-col">            <Fa icon="{faMinus}"/>            <ul>                {#each job.downsizes as downsize}                    <li class="text-gray-700 text-sm">{downsize}</li>                {/each}            </ul>        </div>    </div></div><style></style>